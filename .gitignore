# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri updater signing keys — private key never committed
.tauri-updater-dev.key
.tauri-updater-dev.key.pub
src-tauri/.tauri-updater-dev.key
src-tauri/.tauri-updater-dev.key.pub

# Supabase function secrets and local runtime state — never committed.
supabase/.env
supabase/.branches
supabase/.temp

# Node Compile
node-compile-cache/*
tsx-501/*
.sisyphus/*

# Working notes and agent instructions. Local to whoever is doing the work, and
# not part of what the app is built from.
plans/
AGENTS.md
CLAUDE.md
CHECKLIST.md